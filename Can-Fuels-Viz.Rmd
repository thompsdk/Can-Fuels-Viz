---
title: "Can-Fuels-Viz"
author: "Canadian Forest Service Fire Danger Group"
date: "3/10/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(ggplot2)
NFI_sites <- read.csv("~/Can-Fuels-Viz/NFI_sites.csv")
```

Into text to fuels visualizations

```{r pressure, echo=FALSE}
LoCBH_CFL <- ggplot(NFI_sites, aes(CFL, LoreyCBH)) + geom_point(aes(colour = factor(Dom_ltp_gen))) + scale_x_sqrt(limits=c(0,13), breaks=0:13, expand=c(0,0))
LoCBH_CFL

### want  + geom_density_2d() to show 80% of data by genus for PICE, PINU, PSEU, and ABIE?

```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
